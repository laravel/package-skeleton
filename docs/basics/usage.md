# Usage

Document the primary :package_name workflow here.

Start with the smallest complete example that a Laravel developer can copy into an application:

```php
// Add a focused example for :package_slug once the package API is defined.
```

Follow the example with the expected result and any important behavior users need to understand before using the package in production.

## Common Workflows

Add short sections for the tasks users perform most often. Prefer examples that show :package_name inside a normal Laravel application instead of documenting package internals.

## Advanced Usage

Move optional or less common behavior here so the first example stays easy to scan.
